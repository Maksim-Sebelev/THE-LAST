# Документация библиотеки по работе с AST


Ноды:
Реализован класс BasicNode, являеющийся type erasure для нод.
Сами ноды реализованы как независимые друг от друга и от методов их обработки классы. Так же ноды не имеют привязки к грамматике языка, реализуя лишь свойственный своему названию интерфейс. Например, класс реализующий цикл while выгляди так:
```cpp

```